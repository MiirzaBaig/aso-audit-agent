{
  "name": "aso-audit-agent",
  "private": true,
  "version": "1.0.0",
  "description": "ASO Audit Agent — paste an App Store URL, get a grounded, prioritized App Store Optimization audit. Built with Mastra.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @aso/server --filter @aso/web dev",
    "dev:server": "pnpm --filter @aso/server dev",
    "dev:web": "pnpm --filter @aso/web dev",
    "build": "pnpm --filter @aso/shared build && pnpm --filter @aso/server build && pnpm --filter @aso/web build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @aso/server test",
    "audit:many": "pnpm --filter @aso/server audit:many"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

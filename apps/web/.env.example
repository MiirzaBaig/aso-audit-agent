# Next.js web app env. Copy to apps/web/.env.local (optional).

# Origin of the Mastra server. Defaults to http://localhost:4111 for local dev.
# In production (Vercel) set this to your Railway/Render server URL.
MASTRA_SERVER_URL=http://localhost:4111
